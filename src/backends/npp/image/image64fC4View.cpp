// Code automatically generated by nppCodeGenerator - do not modify manually

#include "image64fC1View.h" //NOLINT
#include "image64fC4View.h" //NOLINT
#ifdef _MSC_VER
#include "../dllexport_npp.h"
#endif
#include "imageView.h"  //NOLINT
#include <backends/cuda/devVarView.h> //NOLINT
#include <backends/npp/nppException.h>  //NOLINT
#include <common/defines.h> //NOLINT
#include <common/exception.h> //NOLINT
#include <common/image/affineTransformation.h> //NOLINT
#include <common/image/border.h> //NOLINT
#include <common/image/bound.h> //NOLINT
#include <common/image/matrix.h> //NOLINT
#include <common/image/pixelTypes.h> //NOLINT
#include <common/image/quad.h> //NOLINT
#include <common/image/roi.h> //NOLINT
#include <common/image/roiException.h>  //NOLINT
#include <common/image/size2D.h>  //NOLINT
#include <common/image/sizePitched.h>  //NOLINT
#include <common/safeCast.h>  //NOLINT
#include <cstddef> //NOLINT
#include <nppdefs.h>  //NOLINT
#include <nppi_geometry_transforms.h> //NOLINT
#include <nppi_statistics_functions.h> //NOLINT

namespace mpp::image::npp
{
#ifdef _MSC_VER
template class MPPEXPORT_NPP ImageView<Pixel64fC4>;
#endif

Image64fC4View::Image64fC4View(const Size2D &aSize) : ImageView<Pixel64fC4>(aSize)
{
}

Image64fC4View::Image64fC4View(Pixel64fC4 *aBasePointer, const SizePitched &aSizeAlloc)
    : ImageView<Pixel64fC4>(aBasePointer, aSizeAlloc)
{
}
Image64fC4View::Image64fC4View(Pixel64fC4 *aBasePointer, const SizePitched &aSizeAlloc, const Roi &aRoi)
    : ImageView<Pixel64fC4>(aBasePointer, aSizeAlloc, aRoi)
{
}

Image64fC4View Image64fC4View::GetView(const Roi &aRoi)
{
    return {Pointer(), SizePitched(SizeAlloc(), Pitch()), aRoi};
}

Image64fC4View Image64fC4View::GetView(const Border &aBorder)
{
    const Roi newRoi = ROI() + aBorder;
    checkRoiIsInRoi(newRoi, Roi(0, 0, SizeAlloc()));
    return {Pointer(), SizePitched(SizeAlloc(), Pitch()), newRoi};
}

//NOLINTBEGIN(readability-identifier-naming,readability-avoid-const-params-in-decls, bugprone-easily-swappable-parameters, readability-convert-member-functions-to-static)

Image64fC4View &Image64fC4View::CrossCorrFull_Norm(const Image64fC4View &pTpl, Image64fC4View &pDst, const NppStreamContext &nppStreamCtx) const
{
    nppSafeCallExt(nppiCrossCorrFull_Norm_64f_C4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), NppiSizeRoi(), reinterpret_cast<const Npp64f *>(pTpl.PointerRoi()), to_int(pTpl.Pitch()), pTpl.NppiSizeRoi(), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pTpl.ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

Image64fC4View &Image64fC4View::CrossCorrFull_NormA(const Image64fC4View &pTpl, Image64fC4View &pDst, const NppStreamContext &nppStreamCtx) const
{
    checkSameSize(ROI(), pDst.ROI());
    nppSafeCallExt(nppiCrossCorrFull_Norm_64f_AC4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), NppiSizeRoi(), reinterpret_cast<const Npp64f *>(pTpl.PointerRoi()), to_int(pTpl.Pitch()), pTpl.NppiSizeRoi(), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pTpl.ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

Image64fC4View &Image64fC4View::CrossCorrSame_Norm(const Image64fC4View &pTpl, Image64fC4View &pDst, const NppStreamContext &nppStreamCtx) const
{
    nppSafeCallExt(nppiCrossCorrSame_Norm_64f_C4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), NppiSizeRoi(), reinterpret_cast<const Npp64f *>(pTpl.PointerRoi()), to_int(pTpl.Pitch()), pTpl.NppiSizeRoi(), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pTpl.ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

Image64fC4View &Image64fC4View::CrossCorrSame_NormA(const Image64fC4View &pTpl, Image64fC4View &pDst, const NppStreamContext &nppStreamCtx) const
{
    checkSameSize(ROI(), pDst.ROI());
    nppSafeCallExt(nppiCrossCorrSame_Norm_64f_AC4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), NppiSizeRoi(), reinterpret_cast<const Npp64f *>(pTpl.PointerRoi()), to_int(pTpl.Pitch()), pTpl.NppiSizeRoi(), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pTpl.ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

Image64fC4View &Image64fC4View::CrossCorrValid_Norm(const Image64fC4View &pTpl, Image64fC4View &pDst, const NppStreamContext &nppStreamCtx) const
{
    nppSafeCallExt(nppiCrossCorrValid_Norm_64f_C4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), NppiSizeRoi(), reinterpret_cast<const Npp64f *>(pTpl.PointerRoi()), to_int(pTpl.Pitch()), pTpl.NppiSizeRoi(), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pTpl.ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

Image64fC4View &Image64fC4View::CrossCorrValid_NormA(const Image64fC4View &pTpl, Image64fC4View &pDst, const NppStreamContext &nppStreamCtx) const
{
    checkSameSize(ROI(), pDst.ROI());
    nppSafeCallExt(nppiCrossCorrValid_Norm_64f_AC4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), NppiSizeRoi(), reinterpret_cast<const Npp64f *>(pTpl.PointerRoi()), to_int(pTpl.Pitch()), pTpl.NppiSizeRoi(), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pTpl.ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

Image64fC4View &Image64fC4View::CrossCorrFull_NormLevel(const Image64fC4View &pTpl, Image64fC4View &pDst, mpp::cuda::DevVarView<byte> &pDeviceBuffer, const NppStreamContext &nppStreamCtx) const
{
    nppSafeCallExt(nppiCrossCorrFull_NormLevel_64f_C4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), NppiSizeRoi(), reinterpret_cast<const Npp64f *>(pTpl.PointerRoi()), to_int(pTpl.Pitch()), pTpl.NppiSizeRoi(), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), pDeviceBuffer.Pointer(), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pTpl.ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

Image64fC4View &Image64fC4View::CrossCorrFull_NormLevelA(const Image64fC4View &pTpl, Image64fC4View &pDst, mpp::cuda::DevVarView<byte> &pDeviceBuffer, const NppStreamContext &nppStreamCtx) const
{
    checkSameSize(ROI(), pDst.ROI());
    nppSafeCallExt(nppiCrossCorrFull_NormLevel_64f_AC4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), NppiSizeRoi(), reinterpret_cast<const Npp64f *>(pTpl.PointerRoi()), to_int(pTpl.Pitch()), pTpl.NppiSizeRoi(), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), pDeviceBuffer.Pointer(), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pTpl.ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

size_t Image64fC4View::FullNormLevelGetBufferHostSize(const NppStreamContext &nppStreamCtx) const
{
    size_t retValue = 0;
    nppSafeCall(nppiFullNormLevelGetBufferHostSize_64f_C4R_Ctx(NppiSizeRoi(), &retValue, nppStreamCtx));
    return retValue;
}

size_t Image64fC4View::FullNormLevelGetBufferHostSizeA(const NppStreamContext &nppStreamCtx) const
{
    size_t retValue = 0;
    nppSafeCall(nppiFullNormLevelGetBufferHostSize_64f_AC4R_Ctx(NppiSizeRoi(), &retValue, nppStreamCtx));
    return retValue;
}

Image64fC4View &Image64fC4View::CrossCorrSame_NormLevel(const Image64fC4View &pTpl, Image64fC4View &pDst, mpp::cuda::DevVarView<byte> &pDeviceBuffer, const NppStreamContext &nppStreamCtx) const
{
    nppSafeCallExt(nppiCrossCorrSame_NormLevel_64f_C4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), NppiSizeRoi(), reinterpret_cast<const Npp64f *>(pTpl.PointerRoi()), to_int(pTpl.Pitch()), pTpl.NppiSizeRoi(), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), pDeviceBuffer.Pointer(), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pTpl.ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

Image64fC4View &Image64fC4View::CrossCorrSame_NormLevelA(const Image64fC4View &pTpl, Image64fC4View &pDst, mpp::cuda::DevVarView<byte> &pDeviceBuffer, const NppStreamContext &nppStreamCtx) const
{
    checkSameSize(ROI(), pDst.ROI());
    nppSafeCallExt(nppiCrossCorrSame_NormLevel_64f_AC4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), NppiSizeRoi(), reinterpret_cast<const Npp64f *>(pTpl.PointerRoi()), to_int(pTpl.Pitch()), pTpl.NppiSizeRoi(), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), pDeviceBuffer.Pointer(), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pTpl.ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

size_t Image64fC4View::SameNormLevelGetBufferHostSize(const NppStreamContext &nppStreamCtx) const
{
    size_t retValue = 0;
    nppSafeCall(nppiSameNormLevelGetBufferHostSize_64f_C4R_Ctx(NppiSizeRoi(), &retValue, nppStreamCtx));
    return retValue;
}

size_t Image64fC4View::SameNormLevelGetBufferHostSizeA(const NppStreamContext &nppStreamCtx) const
{
    size_t retValue = 0;
    nppSafeCall(nppiSameNormLevelGetBufferHostSize_64f_AC4R_Ctx(NppiSizeRoi(), &retValue, nppStreamCtx));
    return retValue;
}

Image64fC4View &Image64fC4View::CrossCorrValid_NormLevel(const Image64fC4View &pTpl, Image64fC4View &pDst, mpp::cuda::DevVarView<byte> &pDeviceBuffer, const NppStreamContext &nppStreamCtx) const
{
    nppSafeCallExt(nppiCrossCorrValid_NormLevel_64f_C4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), NppiSizeRoi(), reinterpret_cast<const Npp64f *>(pTpl.PointerRoi()), to_int(pTpl.Pitch()), pTpl.NppiSizeRoi(), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), pDeviceBuffer.Pointer(), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pTpl.ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

Image64fC4View &Image64fC4View::CrossCorrValid_NormLevelA(const Image64fC4View &pTpl, Image64fC4View &pDst, mpp::cuda::DevVarView<byte> &pDeviceBuffer, const NppStreamContext &nppStreamCtx) const
{
    checkSameSize(ROI(), pDst.ROI());
    nppSafeCallExt(nppiCrossCorrValid_NormLevel_64f_AC4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), NppiSizeRoi(), reinterpret_cast<const Npp64f *>(pTpl.PointerRoi()), to_int(pTpl.Pitch()), pTpl.NppiSizeRoi(), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), pDeviceBuffer.Pointer(), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pTpl.ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

size_t Image64fC4View::ValidNormLevelGetBufferHostSize(const NppStreamContext &nppStreamCtx) const
{
    size_t retValue = 0;
    nppSafeCall(nppiValidNormLevelGetBufferHostSize_64f_C4R_Ctx(NppiSizeRoi(), &retValue, nppStreamCtx));
    return retValue;
}

size_t Image64fC4View::ValidNormLevelGetBufferHostSizeA(const NppStreamContext &nppStreamCtx) const
{
    size_t retValue = 0;
    nppSafeCall(nppiValidNormLevelGetBufferHostSize_64f_AC4R_Ctx(NppiSizeRoi(), &retValue, nppStreamCtx));
    return retValue;
}

Image64fC4View &Image64fC4View::CrossCorrFull_NormLevelAdvanced(const Image64fC4View &pTpl, Image64fC4View &pDst, mpp::cuda::DevVarView<byte> &pDeviceBuffer, mpp::cuda::DevVarView<byte> &pAdvancedScratchBuffer, const NppStreamContext &nppStreamCtx) const
{
    nppSafeCallExt(nppiCrossCorrFull_NormLevelAdvanced_64f_C4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), NppiSizeRoi(), reinterpret_cast<const Npp64f *>(pTpl.PointerRoi()), to_int(pTpl.Pitch()), pTpl.NppiSizeRoi(), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), pDeviceBuffer.Pointer(), pAdvancedScratchBuffer.Pointer(), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pTpl.ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

Image64fC4View &Image64fC4View::CrossCorrSame_NormLevelAdvanced(const Image64fC4View &pTpl, Image64fC4View &pDst, mpp::cuda::DevVarView<byte> &pDeviceBuffer, mpp::cuda::DevVarView<byte> &pAdvancedScratchBuffer, const NppStreamContext &nppStreamCtx) const
{
    nppSafeCallExt(nppiCrossCorrSame_NormLevelAdvanced_64f_C4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), NppiSizeRoi(), reinterpret_cast<const Npp64f *>(pTpl.PointerRoi()), to_int(pTpl.Pitch()), pTpl.NppiSizeRoi(), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), pDeviceBuffer.Pointer(), pAdvancedScratchBuffer.Pointer(), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pTpl.ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

Image64fC4View &Image64fC4View::CrossCorrValid_NormLevelAdvanced(const Image64fC4View &pTpl, Image64fC4View &pDst, mpp::cuda::DevVarView<byte> &pDeviceBuffer, mpp::cuda::DevVarView<byte> &pAdvancedScratchBuffer, const NppStreamContext &nppStreamCtx) const
{
    nppSafeCallExt(nppiCrossCorrValid_NormLevelAdvanced_64f_C4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), NppiSizeRoi(), reinterpret_cast<const Npp64f *>(pTpl.PointerRoi()), to_int(pTpl.Pitch()), pTpl.NppiSizeRoi(), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), pDeviceBuffer.Pointer(), pAdvancedScratchBuffer.Pointer(), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pTpl.ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

void Image64fC4View::MaximumError(const Image64fC4View &pSrc2, mpp::cuda::DevVarView<double> &pError, mpp::cuda::DevVarView<byte> &pDeviceBuffer, const NppStreamContext &nppStreamCtx) const
{
    checkSameSize(ROI(), pSrc2.ROI());
    nppSafeCallExt(nppiMaximumError_64f_C4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), reinterpret_cast<const Npp64f *>(pSrc2.PointerRoi()), to_int(pSrc2.Pitch()), NppiSizeRoi(), pError.Pointer(), pDeviceBuffer.Pointer(), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pSrc2.ROI());
}

size_t Image64fC4View::MaximumErrorGetBufferHostSize(const NppStreamContext &nppStreamCtx) const
{
    size_t retValue = 0;
    nppSafeCall(nppiMaximumErrorGetBufferHostSize_64f_C4R_Ctx(NppiSizeRoi(), &retValue, nppStreamCtx));
    return retValue;
}

void Image64fC4View::AverageError(const Image64fC4View &pSrc2, mpp::cuda::DevVarView<double> &pError, mpp::cuda::DevVarView<byte> &pDeviceBuffer, const NppStreamContext &nppStreamCtx) const
{
    checkSameSize(ROI(), pSrc2.ROI());
    nppSafeCallExt(nppiAverageError_64f_C4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), reinterpret_cast<const Npp64f *>(pSrc2.PointerRoi()), to_int(pSrc2.Pitch()), NppiSizeRoi(), pError.Pointer(), pDeviceBuffer.Pointer(), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pSrc2.ROI());
}

size_t Image64fC4View::AverageErrorGetBufferHostSize(const NppStreamContext &nppStreamCtx) const
{
    size_t retValue = 0;
    nppSafeCall(nppiAverageErrorGetBufferHostSize_64f_C4R_Ctx(NppiSizeRoi(), &retValue, nppStreamCtx));
    return retValue;
}

void Image64fC4View::MaximumRelativeError(const Image64fC4View &pSrc2, mpp::cuda::DevVarView<double> &pError, mpp::cuda::DevVarView<byte> &pDeviceBuffer, const NppStreamContext &nppStreamCtx) const
{
    checkSameSize(ROI(), pSrc2.ROI());
    nppSafeCallExt(nppiMaximumRelativeError_64f_C4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), reinterpret_cast<const Npp64f *>(pSrc2.PointerRoi()), to_int(pSrc2.Pitch()), NppiSizeRoi(), pError.Pointer(), pDeviceBuffer.Pointer(), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pSrc2.ROI());
}

size_t Image64fC4View::MaximumRelativeErrorGetBufferHostSize(const NppStreamContext &nppStreamCtx) const
{
    size_t retValue = 0;
    nppSafeCall(nppiMaximumRelativeErrorGetBufferHostSize_64f_C4R_Ctx(NppiSizeRoi(), &retValue, nppStreamCtx));
    return retValue;
}

void Image64fC4View::AverageRelativeError(const Image64fC4View &pSrc2, mpp::cuda::DevVarView<double> &pError, mpp::cuda::DevVarView<byte> &pDeviceBuffer, const NppStreamContext &nppStreamCtx) const
{
    checkSameSize(ROI(), pSrc2.ROI());
    nppSafeCallExt(nppiAverageRelativeError_64f_C4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), to_int(Pitch()), reinterpret_cast<const Npp64f *>(pSrc2.PointerRoi()), to_int(pSrc2.Pitch()), NppiSizeRoi(), pError.Pointer(), pDeviceBuffer.Pointer(), nppStreamCtx),
                   "ROI Src1: " << ROI() << " ROI Src2: " << pSrc2.ROI());
}

size_t Image64fC4View::AverageRelativeErrorGetBufferHostSize(const NppStreamContext &nppStreamCtx) const
{
    size_t retValue = 0;
    nppSafeCall(nppiAverageRelativeErrorGetBufferHostSize_64f_C4R_Ctx(NppiSizeRoi(), &retValue, nppStreamCtx));
    return retValue;
}

Image64fC4View &Image64fC4View::ResizeSqrPixel(Image64fC4View &pDst, double nXFactor, double nYFactor, double nXShift, double nYShift, int eInterpolation, const NppStreamContext &nppStreamCtx) const
{
    nppSafeCallExt(nppiResizeSqrPixel_64f_C4R_Ctx(reinterpret_cast<const Npp64f *>(Pointer()), NppiSizeFull(), to_int(Pitch()), NppiRectRoi(), reinterpret_cast<Npp64f *>(pDst.Pointer()), to_int(pDst.Pitch()), pDst.NppiRectRoi(), nXFactor, nYFactor, nXShift, nYShift, eInterpolation, nppStreamCtx),
                   "ROI Src: " << ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

Image64fC4View &Image64fC4View::ResizeSqrPixelA(Image64fC4View &pDst, double nXFactor, double nYFactor, double nXShift, double nYShift, int eInterpolation, const NppStreamContext &nppStreamCtx) const
{
    checkSameSize(ROI(), pDst.ROI());
    nppSafeCallExt(nppiResizeSqrPixel_64f_AC4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), NppiSizeFull(), to_int(Pitch()), NppiRectRoi(), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), pDst.NppiRectRoi(), nXFactor, nYFactor, nXShift, nYShift, eInterpolation, nppStreamCtx),
                   "ROI Src: " << ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

void Image64fC4View::ResizeSqrPixel(const Image64fC1View &aSrcChannel0, const Image64fC1View &aSrcChannel1, const Image64fC1View &aSrcChannel2, const Image64fC1View &aSrcChannel3, Image64fC1View &aDstChannel0, Image64fC1View &aDstChannel1, Image64fC1View &aDstChannel2, Image64fC1View &aDstChannel3, double nXFactor, double nYFactor, double nXShift, double nYShift, int eInterpolation, const NppStreamContext &nppStreamCtx)
{
    const Npp64f * srcList[] = { reinterpret_cast<const Npp64f *>(aSrcChannel0.Pointer()), reinterpret_cast<const Npp64f *>(aSrcChannel1.Pointer()), reinterpret_cast<const Npp64f *>(aSrcChannel2.Pointer()), reinterpret_cast<const Npp64f *>(aSrcChannel3.Pointer()) };
    if (aSrcChannel0.Pitch() != aSrcChannel1.Pitch())
    {
        throw INVALIDARGUMENT(aSrcChannel1, "Not all source image planes have the same image pitch. First image pitch is " << aSrcChannel0.Pitch() << " but the pitch for plane no 1 is " << aSrcChannel1.Pitch());
    }
    if (aSrcChannel0.Pitch() != aSrcChannel2.Pitch())
    {
        throw INVALIDARGUMENT(aSrcChannel2, "Not all source image planes have the same image pitch. First image pitch is " << aSrcChannel0.Pitch() << " but the pitch for plane no 2 is " << aSrcChannel2.Pitch());
    }
    if (aSrcChannel0.Pitch() != aSrcChannel3.Pitch())
    {
        throw INVALIDARGUMENT(aSrcChannel3, "Not all source image planes have the same image pitch. First image pitch is " << aSrcChannel0.Pitch() << " but the pitch for plane no 3 is " << aSrcChannel3.Pitch());
    }
    Npp64f * dstList[] = { reinterpret_cast<Npp64f *>(aDstChannel0.PointerRoi()), reinterpret_cast<Npp64f *>(aDstChannel1.PointerRoi()), reinterpret_cast<Npp64f *>(aDstChannel2.PointerRoi()), reinterpret_cast<Npp64f *>(aDstChannel3.PointerRoi()) };
    if (aDstChannel0.Pitch() != aDstChannel1.Pitch())
    {
        throw INVALIDARGUMENT(aDstChannel1, "Not all destination image planes have the same image pitch. First image pitch is " << aDstChannel0.Pitch() << " but the pitch for plane no 1 is " << aDstChannel1.Pitch());
    }
    if (aDstChannel0.Pitch() != aDstChannel2.Pitch())
    {
        throw INVALIDARGUMENT(aDstChannel2, "Not all destination image planes have the same image pitch. First image pitch is " << aDstChannel0.Pitch() << " but the pitch for plane no 2 is " << aDstChannel2.Pitch());
    }
    if (aDstChannel0.Pitch() != aDstChannel3.Pitch())
    {
        throw INVALIDARGUMENT(aDstChannel3, "Not all destination image planes have the same image pitch. First image pitch is " << aDstChannel0.Pitch() << " but the pitch for plane no 3 is " << aDstChannel3.Pitch());
    }
    nppSafeCall(nppiResizeSqrPixel_64f_P4R_Ctx(srcList, aSrcChannel0.NppiSizeFull(), to_int(aSrcChannel0.Pitch()), aSrcChannel0.NppiRectRoi(), dstList, to_int(aDstChannel0.Pitch()), aDstChannel0.NppiRectRoi(), nXFactor, nYFactor, nXShift, nYShift, eInterpolation, nppStreamCtx));
}

Image64fC4View &Image64fC4View::Remap(const Image64fC1View &pXMap, const Image64fC1View &pYMap, Image64fC4View &pDst, int eInterpolation, const NppStreamContext &nppStreamCtx) const
{
    checkSameSize(pXMap.ROI(), pDst.ROI());
    checkSameSize(pYMap.ROI(), pDst.ROI());
    nppSafeCallExt(nppiRemap_64f_C4R_Ctx(reinterpret_cast<const Npp64f *>(Pointer()), NppiSizeFull(), to_int(Pitch()), NppiRectRoi(), reinterpret_cast<const Npp64f *>(pXMap.PointerRoi()), to_int(pXMap.Pitch()), reinterpret_cast<const Npp64f *>(pYMap.PointerRoi()), to_int(pYMap.Pitch()), reinterpret_cast<Npp64f *>(pDst.Pointer()), to_int(pDst.Pitch()), pDst.NppiSizeRoi(), eInterpolation, nppStreamCtx),
                   "ROI Src: " << ROI() << " ROI pXMap: " << pXMap.ROI() << " ROI pYMap: " << pYMap.ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

Image64fC4View &Image64fC4View::RemapA(const Image64fC1View &pXMap, const Image64fC1View &pYMap, Image64fC4View &pDst, int eInterpolation, const NppStreamContext &nppStreamCtx) const
{
    checkSameSize(ROI(), pXMap.ROI());
    checkSameSize(ROI(), pYMap.ROI());
    checkSameSize(ROI(), pDst.ROI());
    nppSafeCallExt(nppiRemap_64f_AC4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), NppiSizeFull(), to_int(Pitch()), NppiRectRoi(), reinterpret_cast<const Npp64f *>(pXMap.PointerRoi()), to_int(pXMap.Pitch()), reinterpret_cast<const Npp64f *>(pYMap.PointerRoi()), to_int(pYMap.Pitch()), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), pDst.NppiSizeRoi(), eInterpolation, nppStreamCtx),
                   "ROI Src: " << ROI() << " ROI pXMap: " << pXMap.ROI() << " ROI pYMap: " << pYMap.ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

void Image64fC4View::Remap(const Image64fC1View &aSrcChannel0, const Image64fC1View &aSrcChannel1, const Image64fC1View &aSrcChannel2, const Image64fC1View &aSrcChannel3, Image64fC1View &aDstChannel0, Image64fC1View &aDstChannel1, Image64fC1View &aDstChannel2, Image64fC1View &aDstChannel3, const Image64fC1View &pXMap, const Image64fC1View &pYMap, int eInterpolation, const NppStreamContext &nppStreamCtx)
{
    const Npp64f * srcList[] = { reinterpret_cast<const Npp64f *>(aSrcChannel0.Pointer()), reinterpret_cast<const Npp64f *>(aSrcChannel1.Pointer()), reinterpret_cast<const Npp64f *>(aSrcChannel2.Pointer()), reinterpret_cast<const Npp64f *>(aSrcChannel3.Pointer()) };
    if (aSrcChannel0.Pitch() != aSrcChannel1.Pitch())
    {
        throw INVALIDARGUMENT(aSrcChannel1, "Not all source image planes have the same image pitch. First image pitch is " << aSrcChannel0.Pitch() << " but the pitch for plane no 1 is " << aSrcChannel1.Pitch());
    }
    if (aSrcChannel0.Pitch() != aSrcChannel2.Pitch())
    {
        throw INVALIDARGUMENT(aSrcChannel2, "Not all source image planes have the same image pitch. First image pitch is " << aSrcChannel0.Pitch() << " but the pitch for plane no 2 is " << aSrcChannel2.Pitch());
    }
    if (aSrcChannel0.Pitch() != aSrcChannel3.Pitch())
    {
        throw INVALIDARGUMENT(aSrcChannel3, "Not all source image planes have the same image pitch. First image pitch is " << aSrcChannel0.Pitch() << " but the pitch for plane no 3 is " << aSrcChannel3.Pitch());
    }
    Npp64f * dstList[] = { reinterpret_cast<Npp64f *>(aDstChannel0.PointerRoi()), reinterpret_cast<Npp64f *>(aDstChannel1.PointerRoi()), reinterpret_cast<Npp64f *>(aDstChannel2.PointerRoi()), reinterpret_cast<Npp64f *>(aDstChannel3.PointerRoi()) };
    if (aDstChannel0.Pitch() != aDstChannel1.Pitch())
    {
        throw INVALIDARGUMENT(aDstChannel1, "Not all destination image planes have the same image pitch. First image pitch is " << aDstChannel0.Pitch() << " but the pitch for plane no 1 is " << aDstChannel1.Pitch());
    }
    if (aDstChannel0.Pitch() != aDstChannel2.Pitch())
    {
        throw INVALIDARGUMENT(aDstChannel2, "Not all destination image planes have the same image pitch. First image pitch is " << aDstChannel0.Pitch() << " but the pitch for plane no 2 is " << aDstChannel2.Pitch());
    }
    if (aDstChannel0.Pitch() != aDstChannel3.Pitch())
    {
        throw INVALIDARGUMENT(aDstChannel3, "Not all destination image planes have the same image pitch. First image pitch is " << aDstChannel0.Pitch() << " but the pitch for plane no 3 is " << aDstChannel3.Pitch());
    }
    checkSameSize(pXMap.ROI(), aDstChannel0.ROI());
    checkSameSize(pYMap.ROI(), aDstChannel0.ROI());
    nppSafeCall(nppiRemap_64f_P4R_Ctx(srcList, aSrcChannel0.NppiSizeFull(), to_int(aSrcChannel0.Pitch()), aSrcChannel0.NppiRectRoi(), reinterpret_cast<const Npp64f *>(pXMap.PointerRoi()), to_int(pXMap.Pitch()), reinterpret_cast<const Npp64f *>(pYMap.PointerRoi()), to_int(pYMap.Pitch()), dstList, to_int(aDstChannel0.Pitch()), aDstChannel0.NppiSizeRoi(), eInterpolation, nppStreamCtx));
}

Image64fC4View &Image64fC4View::WarpAffine(Image64fC4View &pDst, const AffineTransformation<double> &aCoeffs, int eInterpolation, const NppStreamContext &nppStreamCtx) const
{
    nppSafeCallExt(nppiWarpAffine_64f_C4R_Ctx(reinterpret_cast<const Npp64f *>(Pointer()), NppiSizeFull(), to_int(Pitch()), NppiRectRoi(), reinterpret_cast<Npp64f *>(pDst.Pointer()), to_int(pDst.Pitch()), pDst.NppiRectRoi(), reinterpret_cast<const double(*)[3]>(&aCoeffs), eInterpolation, nppStreamCtx),
                   "ROI Src: " << ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

Image64fC4View &Image64fC4View::WarpAffineA(Image64fC4View &pDst, const AffineTransformation<double> &aCoeffs, int eInterpolation, const NppStreamContext &nppStreamCtx) const
{
    checkSameSize(ROI(), pDst.ROI());
    nppSafeCallExt(nppiWarpAffine_64f_AC4R_Ctx(reinterpret_cast<const Npp64f *>(PointerRoi()), NppiSizeFull(), to_int(Pitch()), NppiRectRoi(), reinterpret_cast<Npp64f *>(pDst.PointerRoi()), to_int(pDst.Pitch()), pDst.NppiRectRoi(), reinterpret_cast<const double(*)[3]>(&aCoeffs), eInterpolation, nppStreamCtx),
                   "ROI Src: " << ROI() << "ROI Dst: " << pDst.ROI());
    return pDst;
}

void Image64fC4View::WarpAffine(const Image64fC1View &aSrcChannel0, const Image64fC1View &aSrcChannel1, const Image64fC1View &aSrcChannel2, const Image64fC1View &aSrcChannel3, Image64fC1View &aDstChannel0, Image64fC1View &aDstChannel1, Image64fC1View &aDstChannel2, Image64fC1View &aDstChannel3, const AffineTransformation<double> &aCoeffs, int eInterpolation, const NppStreamContext &nppStreamCtx)
{
    const Npp64f * srcList[] = { reinterpret_cast<const Npp64f *>(aSrcChannel0.Pointer()), reinterpret_cast<const Npp64f *>(aSrcChannel1.Pointer()), reinterpret_cast<const Npp64f *>(aSrcChannel2.Pointer()), reinterpret_cast<const Npp64f *>(aSrcChannel3.Pointer()) };
    if (aSrcChannel0.Pitch() != aSrcChannel1.Pitch())
    {
        throw INVALIDARGUMENT(aSrcChannel1, "Not all source image planes have the same image pitch. First image pitch is " << aSrcChannel0.Pitch() << " but the pitch for plane no 1 is " << aSrcChannel1.Pitch());
    }
    if (aSrcChannel0.Pitch() != aSrcChannel2.Pitch())
    {
        throw INVALIDARGUMENT(aSrcChannel2, "Not all source image planes have the same image pitch. First image pitch is " << aSrcChannel0.Pitch() << " but the pitch for plane no 2 is " << aSrcChannel2.Pitch());
    }
    if (aSrcChannel0.Pitch() != aSrcChannel3.Pitch())
    {
        throw INVALIDARGUMENT(aSrcChannel3, "Not all source image planes have the same image pitch. First image pitch is " << aSrcChannel0.Pitch() << " but the pitch for plane no 3 is " << aSrcChannel3.Pitch());
    }
    Npp64f * dstList[] = { reinterpret_cast<Npp64f *>(aDstChannel0.PointerRoi()), reinterpret_cast<Npp64f *>(aDstChannel1.PointerRoi()), reinterpret_cast<Npp64f *>(aDstChannel2.PointerRoi()), reinterpret_cast<Npp64f *>(aDstChannel3.PointerRoi()) };
    if (aDstChannel0.Pitch() != aDstChannel1.Pitch())
    {
        throw INVALIDARGUMENT(aDstChannel1, "Not all destination image planes have the same image pitch. First image pitch is " << aDstChannel0.Pitch() << " but the pitch for plane no 1 is " << aDstChannel1.Pitch());
    }
    if (aDstChannel0.Pitch() != aDstChannel2.Pitch())
    {
        throw INVALIDARGUMENT(aDstChannel2, "Not all destination image planes have the same image pitch. First image pitch is " << aDstChannel0.Pitch() << " but the pitch for plane no 2 is " << aDstChannel2.Pitch());
    }
    if (aDstChannel0.Pitch() != aDstChannel3.Pitch())
    {
        throw INVALIDARGUMENT(aDstChannel3, "Not all destination image planes have the same image pitch. First image pitch is " << aDstChannel0.Pitch() << " but the pitch for plane no 3 is " << aDstChannel3.Pitch());
    }
    nppSafeCall(nppiWarpAffine_64f_P4R_Ctx(srcList, aSrcChannel0.NppiSizeFull(), to_int(aSrcChannel0.Pitch()), aSrcChannel0.NppiRectRoi(), dstList, to_int(aDstChannel0.Pitch()), aDstChannel0.NppiRectRoi(), reinterpret_cast<const double(*)[3]>(&aCoeffs), eInterpolation, nppStreamCtx));
}

//NOLINTEND(readability-identifier-naming,readability-avoid-const-params-in-decls, bugprone-easily-swappable-parameters, readability-convert-member-functions-to-static)
} // namespace mpp::image::npp
