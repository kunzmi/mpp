#pragma once
#include "defines.h"
// #include "numberTypes.h"
#include <concepts>
#include <type_traits>

namespace opp
{
// forward declaration
// template <SignedNumber T> struct Complex;

// template <typename T>
// concept ComplexOrNumber = Number<T> || ComplexType<T>;
//
// template <typename T>
// concept IntComplexType = isComplexType<T>::value && Integral<complex_basetype_t<T>>;
//
// template <typename T>
// concept FloatingComplexType = isComplexType<T>::value && FloatingPoint<complex_basetype_t<T>>;
} // namespace opp
